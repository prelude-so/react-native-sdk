import PreludeReactNativeSdkStandalone from './NativePreludeReactNativeSdkStandalone';

// Native (iOS/Android) entry point. Bundlers resolve the sibling `.web.ts`
// on web targets, so this file never loads there.
export default PreludeReactNativeSdkStandalone;
